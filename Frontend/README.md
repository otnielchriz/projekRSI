# KerjoLe Frontend

React + Vite frontend.

## Jalankan

```powershell
npm install
npm run dev
```

Backend API dibaca dari `.env`:

```env
VITE_API_URL=http://localhost:8000
```

API service ada di `src/app/services/api.ts`.
